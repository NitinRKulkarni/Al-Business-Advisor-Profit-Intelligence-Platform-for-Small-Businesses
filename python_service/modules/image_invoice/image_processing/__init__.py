"""
image_processing
=================

Core computer-vision preprocessing pipeline for handwritten invoice images.

This package is intentionally scoped to image preprocessing only. It does not
perform OCR, LLM extraction, database access, or API serving — those live in
sibling packages (ocr/, extraction/, validation/, api/) added in later phases.
"""
